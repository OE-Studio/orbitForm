const { Admin } = require("../model/admin.model");
const { getSignedToken } = require("../../utils/sendToken");
const bcrypt = require('bcrypt');

const createWorker = async(worker) => {
    try{
        const existingWorker = await Admin.find
    }
    catch{}
}